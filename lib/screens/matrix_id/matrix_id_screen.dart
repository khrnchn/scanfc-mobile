import 'package:flutter/material.dart';

class MatrixIDScreen extends StatefulWidget {
  const MatrixIDScreen({super.key});

  @override
  State<MatrixIDScreen> createState() => _MatrixIDScreenState();
}

class _MatrixIDScreenState extends State<MatrixIDScreen> {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Matrix id screen")
      ],
    );
  }
}